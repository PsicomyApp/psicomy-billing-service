name: Build and Deploy Billing Service

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      deploy_prd:
        description: 'Deploy directly to PRD (skip HML)'
        required: false
        default: false
        type: boolean

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: psicomyapp/psicomy-billing-service
  SERVICE_NAME: billing-service

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha,prefix=
            type=raw,value=hml-latest,enable={{is_default_branch}}
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./src
          file: ./src/Dockerfile
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            GITHUB_TOKEN=${{ secrets.NUGET_AUTH_TOKEN }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy-hml:
    needs: build
    runs-on: ubuntu-latest
    if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main' && !inputs.deploy_prd
    environment: hml

    steps:
      - name: Deploy to HML Server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.HML_SSH_HOST }}
          username: ${{ secrets.HML_SSH_USERNAME }}
          key: ${{ secrets.HML_SSH_KEY }}
          port: 22
          script: |
            set -e
            echo "🚀 Deploying billing-service to HML..."

            # Login to GitHub Container Registry
            echo ${{ secrets.GITHUB_TOKEN }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin

            # Pull the HML image
            docker pull ghcr.io/psicomyapp/psicomy-billing-service:hml-latest

            # Navigate to HML directory
            cd /opt/psicomy/hml

            # Update service via docker-compose
            docker compose pull billing-service
            docker compose up -d --no-deps --force-recreate billing-service

            # Cleanup old images
            docker image prune -af --filter "until=168h"

            echo "✅ billing-service deployed to HML"

      - name: Health Check HML
        run: |
          echo "⏳ Waiting for service to start..."
          sleep 30
          curl -sf https://backend-hml.psicomy.com.br/api/billing/health || echo "⚠️ Service starting..."

  deploy-prd:
    needs: [build, deploy-hml]
    runs-on: ubuntu-latest
    if: always() && needs.build.result == 'success' && (needs.deploy-hml.result == 'success' || needs.deploy-hml.result == 'skipped')
    environment: prd

    steps:
      - name: Deploy to PRD Server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.PRD_SSH_HOST }}
          username: ${{ secrets.PRD_SSH_USERNAME }}
          key: ${{ secrets.PRD_SSH_KEY }}
          port: 22
          script: |
            set -e
            echo "🚀 Deploying billing-service to PRD..."

            # Login to GitHub Container Registry
            echo ${{ secrets.GITHUB_TOKEN }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin

            # Pull the latest image
            docker pull ghcr.io/psicomyapp/psicomy-billing-service:latest

            # Navigate to PRD directory
            cd /opt/psicomy/prd

            # Update service via docker-compose
            docker compose pull billing-service
            docker compose up -d --no-deps --force-recreate billing-service

            # Cleanup old images
            docker image prune -af --filter "until=168h"

            echo "✅ billing-service deployed to PRD"

      - name: Health Check PRD
        run: |
          echo "⏳ Waiting for service to start..."
          sleep 30
          curl -sf https://backend.psicomy.com.br/api/billing/health || echo "⚠️ Service starting..."

  notify:
    needs: [build, deploy-hml, deploy-prd]
    runs-on: ubuntu-latest
    if: always() && github.event_name != 'pull_request'

    steps:
      - name: Summary
        run: |
          echo "### Deployment Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Stage | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|-------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| Build | ${{ needs.build.result == 'success' && '✅' || '❌' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| HML | ${{ needs.deploy-hml.result == 'success' && '✅' || needs.deploy-hml.result == 'skipped' && '⏭️' || '❌' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| PRD | ${{ needs.deploy-prd.result == 'success' && '✅' || needs.deploy-prd.result == 'skipped' && '⏭️' || '❌' }} |" >> $GITHUB_STEP_SUMMARY
